{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col">
            <h1>
                {% if user %}
                    Hello {{ user.userIdentifier }}!
                {% endif %}
            </h1>
            <p>{{ message }}</p>
        </div>
    </div>
</div>
{% endblock %}